import React from "react";
import ReactDOM from "react-dom";
// import { createRoot } from "react-dom/client"; // Remove this line since createRoot is not used
import App from "./App";
import "./index.css";

ReactDOM.render(<App />, document.getElementById("root")); // Render App directly using ReactDOM.render
